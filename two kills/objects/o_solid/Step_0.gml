step();
if hp != infinity {
	c_basichurting();
}
if !hp kys;
if instance_exists(o_mapper) exit;
//value = clamp(value-24, 0, 256);
//depth = -abs(depth);

//if collision_point(x-9, y, df, false, false) {
	//df.x--;
//}
//depth = -8;